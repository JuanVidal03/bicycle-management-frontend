import useDocumentTitle from '../../hooks/useDocumentTitle.ts';

const Index = () => {
  useDocumentTitle('Eventos');

  return (
    <div>
      Events      
    </div>
  );
};

export default Index;
